// Configuración de URLs externas
const AppConfig = {
    // URL del sistema de reservas
    reservasUrl: 'https://reserva.great-site.net/',
    
    // URL del sistema de gestión escolar
    sistemaUrl: './sge-no-disponible',
    
    // URL del formulario de inscripción
    inscripcionUrl: ''
};

// Hacer la configuración disponible globalmente
window.AppConfig = AppConfig;
